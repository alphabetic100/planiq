import 'package:flutter/material.dart';

class EmployeJobsScreen extends StatelessWidget {
  const EmployeJobsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Jobs"),
      ),
    );
  }
}
