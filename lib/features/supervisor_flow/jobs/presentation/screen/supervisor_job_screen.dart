import 'package:flutter/material.dart';

class SupervisorJobScreen extends StatelessWidget {
  const SupervisorJobScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Supervisor Job Screen"),
      ),
    );
  }
}
